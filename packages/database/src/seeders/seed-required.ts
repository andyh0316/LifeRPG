import type { Db } from './types.js';

/** Runs all required seeders that insert reference/lookup data the app needs to function. */
export async function runRequiredSeeders(_db: Db) {
  // No required seeders at this time
}
